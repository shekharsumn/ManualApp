import 'package:flutter/material.dart';

/// A static class to manage all app colors.
class AppColors {
  // Primary color for the app (e.g., used in buttons, app bar)
  static const Color primaryColor = Color(0xFFA5B79F);

  // Secondary color (e.g., for accent buttons, highlights)
  static const Color secondaryColor = Color(0xFF0B3B3C);

  // Background color for screens
  static const Color backgroundColor = Color(0xFFECF0EB);

  // Text color for headlines
  static const Color headlineTextColor = Color(0xFF212121);

  // Text color for body text
  static const Color bodyTextColor = Color(0xFF757575);

  // Border color (e.g., for input fields, dividers)
  static const Color borderColor = Color(0xFFE0E0E0);

  // Error color (e.g., for error messages or input validation)
  static const Color errorColor = Color(0xFFD32F2F);

  // Success color (e.g., for success messages or valid inputs)
  static const Color successColor = Color(0xFF388E3C);

  // Disabled button or text color
  static const Color disabledColor = Color(0xFFBDBDBD);

  // Accent color (e.g., for icons or text accents)
  static const Color accentColor = Color(0xFFFF5722);

  // Transparent color (used for backgrounds, shadows, etc.)
  static const Color transparent = Colors.transparent;

  // Button Text color - maroon
  static const Color darkRedColor = Color(0xFF7E0707);

  // Disabled Button color
  static const Color disbledBtnColor = Color(0xFFBFCBC8);

  // Background color - lime
  static const Color backgroundLimeColor = Color(0xFFEBF0EB);

  static const Color titleColor = Color(0xFF6D8A83);
  static const Color gridSelectedColor = Color(0xFFA5B79F);
}